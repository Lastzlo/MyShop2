#postgres
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost/myshop2}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:12345}
spring.jpa.generate-ddl=true

#port
server.port=9500

#path for save picture
#upload.picture.path=C:/JavaProjects/other/forProjects/myshop1

#path for unload picture
unload.picture.path=/img/

#чтобы можно было загрузить по больше изображений за один раз
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
